#### V provides `.filter()`, `.map()`, `.any()`, and `.all()` as built-in array methods with `it` as the implicit element variable.
___
##### Run Command:

`$ v run collection-functions.v`

##### Results:

`2`
`false`
`true`
`false`
`['peach', 'apple', 'pear']`
`['PEACH', 'APPLE', 'PEAR', 'PLUM']`

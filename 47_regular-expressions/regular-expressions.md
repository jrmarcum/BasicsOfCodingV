#### V's `regex` module provides pattern matching, finding, and replacing. The API differs from Go's `regexp` package.
___
##### Run Command:

`$ v run regular-expressions.v`

##### Results:

`true`
`peach`
`['peach', 'pinch']`
`peach`
`pinch`
`a <fruit>`

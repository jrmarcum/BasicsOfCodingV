#### In V, string operations are methods on the string type rather than package functions. `index()` returns `?int` (an optional).
___
##### Run Command:

`$ v run string-functions.v`

##### Results:

`Contains:  true`
`Count:     2`
`HasPrefix: true`
`HasSuffix: true`
`Index:     1`
`Join:      a-b`
`Repeat:    aaaaa`
`Replace:   f00`
`Replace:   f0o`
`Split:     ['a', 'b', 'c', 'd', 'e']`
`ToLower:   test`
`ToUpper:   TEST`
``
`Len: 5`
`Char: 101`

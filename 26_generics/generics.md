#### V generics use `[T]` syntax; the linked-list example requires `unsafe` for raw pointer operations.
___
##### Run Command:

`$ v run generics.v`

##### Results:

`index of zoo: 2`
`list: [10, 13, 23]`

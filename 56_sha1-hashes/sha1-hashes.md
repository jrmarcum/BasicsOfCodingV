#### V's `crypto.sha1` module computes SHA1 hashes via `sha1.sum()`. Use `.hex()` on the resulting byte slice to get the hex string.
___
##### Run Command:

`$ v run sha1-hashes.v`

##### Results:

`sha1 this string`
`cf23df2207d99a74fbe169e3eba035e633b65d94`

#### Running this program will cause it to panic, print an error message and goroutine traces, and exit with a non-zero status.
___
##### Run Command:

`$ v run panic.v`

##### Results:

`V panic: a problem`
`v/panic.v:10 main.main (./panic.v)`

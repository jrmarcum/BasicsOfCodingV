#### V's `time` module supports creating, comparing, and doing arithmetic on times. V times are always UTC-based; there is no separate Location type.
___
##### Run Command:

`$ v run time.v`

##### Results:

`2009-11-17 20:34:58`
`2009`
`11`
`17`
`20`
`34`
`58`
`651387237`
`UTC`
`tuesday`
`true`
`false`
`false`
`... (duration string varies by run time)`
`... (hours as f64 varies)`
`... (minutes as f64 varies)`
`... (seconds as f64 varies)`
`... (nanoseconds as i64 varies)`
`... (then + diff varies)`
`... (then - diff varies)`

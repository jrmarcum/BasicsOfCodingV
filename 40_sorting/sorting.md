#### Running our program prints the sorted string and int arrays and `true` as the result of our sorted test. V prints arrays with brackets and commas.
___
##### Run Command:

`$ v run sorting.v`

##### Results:

`Strings: ['a', 'b', 'c']`
`Ints:    [2, 4, 7]`
`Sorted:  true`

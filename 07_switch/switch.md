#### V uses `match` instead of `switch`. Time-dependent output (weekday, hour) will vary when run.
___
##### Run Command:

`$ v run switch.v`

##### Results:

`Write 2 as two`
`It's a weekday`
`It's after noon`
`bool true`
`int 1`
`unknown type string`

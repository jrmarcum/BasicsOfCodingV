#### V's `os` module provides file writing via `os.write_file()` for whole-file writes and `os.File` methods for granular byte and string writes. Create the `./tmp/` directory before running.
___
##### Run Command:

`$ v run writing-files.v`

##### Results:

`wrote 5 bytes`
`wrote 7 bytes`
`wrote 9 bytes`

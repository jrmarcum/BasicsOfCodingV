#### V has no `recover` built-in. The closest idiom uses `!T` Result types and `or { }` blocks instead of Go's deferred `recover()` pattern.
___
##### Run Command:

`$ v run recover.v`

##### Results:

`Recovered. Error:`
` a problem`

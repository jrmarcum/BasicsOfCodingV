#### V reads stdin line by line using `io.new_buffered_reader`. Each line is uppercased and printed to stdout.
___
##### Run Command:

`$ echo 'hello\nfilter' | v run line-filters.v`

##### Results:

`HELLO`
`FILTER`
